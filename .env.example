# AI 在线生成图标 —— 服务端配置
# 复制本文件为 .env 并填入真实密钥；.env 已被 .gitignore 忽略，不会入库

# AI 服务商：仅支持 openai（包括 OpenAI 兼容接口）
# openai provider 兼容任何 OpenAI 协议接口（如 DeepSeek、通义、本地 vLLM/Ollama）
AI_PROVIDER=openai

# 兼容接口的可选项（默认 openai 官方）；用 DeepSeek V4 时如下：
OPENAI_BASE_URL=https://api.deepseek.com/v1
OPENAI_MODEL=deepseek-v4-pro

# 图标风格固定为三套（扁平双色 / 渐变质感 / 线性淡彩），每次输入名称各生成 1 张，无需配置数量

OPENAI_API_KEY=
